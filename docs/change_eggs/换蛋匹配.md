换蛋广场的功能:
需要添加接口
1. 用户可以上传自己拥有的蛋组信息，比如，可以选择 pokemon_egg的蛋组信息、如果蛋组没有则可以直接从pokemon中选择拥有的宠物蛋，因为无论选什么数据库应该存储对应的pokemon_id，然后用户还可以选择。对应的蛋组的tag 这个tag应该由后台配置可以选的tag 比如有 大块头、小不点等等，然后用户还需要选择 自己希望的蛋组信息 同样的信息；一个是拥有的 一个是需求的，然后一个字段用于记录用户的游戏id长串的数字

2. 应该有一个定时任务，去持续的遍历数据去查询需求和被需求相互吻合的用户 然后给双方下发通知（先预留逻辑）
2. 用户提交之后，应该立即触发一次查询（应该是异步的），也就是定时任务的逻辑，然后去库中匹配 如何可以匹配则立即下发通知（通知逻辑可以先预留）

需要的表应该记录在 @sql 文件夹下 然后我
# 需求二
需要帮我实现一个用户之间发送消息的功能,
如果A发送消息给B用户 首先会记录在数据库中 并且如果通过websocket实时发送消息到B用户的客户端，仅支持私聊，如果B用户不在线，则需要记录在数据库中，等待B用户上线之后，从数据库校验一下是否有未接收的数据 然后同样通过websocket下发出去

可以将这些功能封装为通用功能，我下发换蛋通知的时候，有统一入口直接

需要添加一个用户可以相互沟通的逻辑，主要通过websocket和db,如果用户在线 下发通知的时候应该立即通过websocket发送通知
如果对方用户不在线，则应该将消息记录在数据库中，用户上线后检查数据库是否有对应的消息然后下发通知

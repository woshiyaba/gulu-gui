# 宠物对话功能
## 需求
- 需求添加一个prompt表，每个宠物需要绑定一个独立的prompt 有宠物id和 prompt 
- 需要添加一个prompt补充表，需要有用户id和宠物id 用户可以在有prompt前提下 去配置一些宠物特定属性 比如性格、昵称、伙伴关系等等希望字段是可扩展的，在prompt进行补充
- 建表语句在 @sql 目录下添加，我手动进行创建
- 补充 @chat_agent.py 的agent方法
- 每一个用户与每一只宠物需要保持一个聊天的websocket，在用户点击宠物聊天的时候，需要查询对应宠物prompt和用户自定义宠物的相关扩展补充，合并为一个system prompt 然后创建对应的agent与用户聊天
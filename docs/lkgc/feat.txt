 @docs/lkgc/getList_exam.json 这个是 @common/utils/lkgc.py:118-131
  fetch_pet_list的返回值 @docs/lkgc/getPetInfo_exam.json 是
  @common/utils/lkgc.py:134-136获取宠物详情的返回值 请求示例 在
  @docs/lkgc/example.txt 帮我在 @api/schemas/ops.py 中写一个方法
  分页获取宠物列表 然后遍历宠物列表获取详情 获取宠物的详情  1.根据
  getListexam的 name和from 组合为 name（from_name）去数库pokemon.name 中查询   
  如果有则不做任何操作，本次操作只插入不更新历史数据 然后将main_url 上传到oss  
  将作为pokemon.image_lc，mf->mdef,mg->matk,sd->spd,sm->hp,wf->def_val,wg->atk 这是种族值的对应关系，
  catch_way->obtain_method avatar_url->avatar有的话则更新 没有的话则不更新 将数据插入到pokemon

 2. 在精灵维护列表的操作列增加“同步技能”功能，点击后弹出同步结果弹窗。后端根据当前 pokemon.name 匹配
 getList 返回的 name/from 组合名，再调用 getPetInfo 获取详情，读取 skill_list 中的 base、bloodline、stone 三类技能。
 base 写入 pokemon_skill.type=原生技能，bloodline 写入 pokemon_skill.type=血脉技能，stone 写入 pokemon_skill.type=技能石技能。
 同步时先按 skill.name 查询本库 skill 表：如果存在，只复用 skill.id，不更新已有 skill 的属性、威力、能耗、描述、图标；
 如果不存在，则新增 skill 后再写入 pokemon_skill。新增 skill 时 take_energy->consume，power->power，
 desc->skill_desc，icon_url 先上传到 oss 后写入 icon；技能属性 attr_id 不使用 lkgc 的 type_id，新增技能先写 NULL，
 后续以本库维护的 skill.attr_id 为准。
 技能类型 skill.type 优先使用本库已有值；新增技能可按 lkgc 的 category_id + damage_id 推断：
 category_id=1 且 damage_id=1 -> 物攻，category_id=1 且 damage_id=2 -> 魔攻，
 category_id=2 且 damage_id=-1 -> 防御，category_id=3 且 damage_id=-1 -> 状态。
 tag_id 不作为技能类型映射依据。pokemon_skill 按 (pokemon_id, skill_id) 幂等写入，重复同步时更新关系的 type 和 sort_order。
 弹窗展示请求技能数、命中/新增技能数、新增/更新关系数、跳过数、告警信息和逐条技能处理结果。
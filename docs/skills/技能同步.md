1. 帮我编写一个脚本 将 @docs/skills/skill-mini-map.json 中的数据入库 他们是技能和特性表，如果families 为 SDT_NONE,则为特性，需要根据name去pokemon_trait表中查询是否有相同的pokemon_trait.name 如果没有则新增一条 name为pokemon_trait.name desc为pokemon_trait.description 

2.如果不为SDT_NONE 则需要写入到skill表中 使 families的值从attribute中根据name查询 attribute.name=families的值 获取到对应属性的id 为skill.attr_id、name为skill.name、power为skill.power、cost为skill.consume、desc为skill.skill_desc skill.icon 使用 https://game.gtimg.cn/images/rocom/rocodata/skill/7080380.png，最后的图片是根据每个字典的key来拼接，需要上传到自己的oss 然后写入到数据库中，如果url图片为404则打印日志，这条数据不入库，入库的时候需要根据json的name去skill.name查询对应的数据，如果有则不更新，如果没有则添加